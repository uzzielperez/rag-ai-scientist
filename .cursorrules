You are working in `rag-ai-scientist`.

Priorities:
- Keep changes reproducible and script-driven.
- Prefer updating scripts/configs/docs in this repo over ad-hoc commands.
- Preserve deterministic behavior for validation outputs.

RAG and Cursor:
- Use `.cursor/rag_db` as the local vector DB path.
- Use `.cursor/run_mcp_server.sh` to start MCP for this repo.
- Keep `.cursor` wrappers aligned with `rag/*.py` implementations.

Safety:
- Do not remove or overwrite run artifacts unless explicitly requested.
- Do not hardcode secrets; use environment variables for API keys.
