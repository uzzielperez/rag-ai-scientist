name: Dev Specs Branch Guard

on:
  pull_request:
  push:

jobs:
  guard-dev-only-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Enforce dev-only specs docs policy
        run: bash scripts/check_dev_specs_branch.sh
